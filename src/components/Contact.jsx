import Form from "../components/Form";

function Contact() {
    return (
        <section id="contact">
        <div className="flex">
            <Form />
        </div>
        </section>
    )
}

export default Contact